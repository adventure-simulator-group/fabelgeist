# Independent candidate 21 code review

Bounded source review on 2026-09-09. No geometry or implementation edits were
made. The existing rounded neutral audit and its two witness triangles were
inspected; geometry was not regenerated by this reviewer.

No new concrete P1/P2 indexing or morph-correspondence defect identified in
`seating.rs`. At its current call site, front and back both contain coarse
49-column main/skirt rows; seating runs before front flute refinement. Removing
the shoulder rows is reflected in skirt sampling offsets and medial metadata.
The two seating passes affect disjoint lateral strips, retain vertex heights,
and leave connectivity fixed. Height lookup uses the sorted front side edge,
with bounded interpolation and endpoint extension. The arithmetic consistently
uses the wearer frame for front-edge and radial-direction coordinates.

The 3 mm radial offset and 5 mm forward overlap describe the construction
target, not a measured minimum separation between final triangles. A radial
direction is not necessarily the front wall's extrusion normal, and the back
wall is solidified after its return is moved. Consequently the final shell
contacts, particularly across thickness extremes and morph blends, still need
the objective checks. This source review does not certify geometric clearance.

**Current geometry acceptance remains blocked:** the implementation owner's
`21-rounded-check.log` records one nonadjacent intersection, faces 14004 and
15852, on the rear upper outer surface and rim at approximately 1.41 m body
height. That region is above the lower seating fade. The audit records zero
body triangle intersections and zero body samples penetrating by over 1 mm,
but this remaining shell intersection prevents a geometry pass.

Checks performed by this reviewer:

- `cargo clippy -p adventuresim-armor-model --all-targets -- -D warnings`:
  passed, including compilation of the crate and test targets.
- `cargo fmt --all -- --check`: failed solely on the inspected formatting
  differences in the new `seating.rs`; no formatting was applied.
- `git diff --check`: passed.
- No remaining shoulder-width API, band constants, or band-builder references
  found in searched current source/scripts. Module prose in
  `breastplate_carrier.rs` and `grid.rs` still describes shoulder-band generation
  and should be updated. Historical review statements remain historical evidence.

Final workspace quality checks and the new candidate's neutral/morph/visual
acceptance remain the implementation owner's responsibility. The earlier
candidate 20 source verdict does not override its subsequently failed geometry
or visual evidence.
