// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::surrender_action_kind_type::SurrenderActionKind;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ResolveContextSurrenderArgs {
    pub actor_id: u64,
    pub target_id: u64,
    pub contact_ref: String,
    pub action: SurrenderActionKind,
    pub expected_revision: u32,
    pub source_id: String,
}

impl From<ResolveContextSurrenderArgs> for super::Reducer {
    fn from(args: ResolveContextSurrenderArgs) -> Self {
        Self::ResolveContextSurrender {
            actor_id: args.actor_id,
            target_id: args.target_id,
            contact_ref: args.contact_ref,
            action: args.action,
            expected_revision: args.expected_revision,
            source_id: args.source_id,
        }
    }
}

impl __sdk::InModule for ResolveContextSurrenderArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `resolve_context_surrender`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait resolve_context_surrender {
    /// Request that the remote module invoke the reducer `resolve_context_surrender` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`resolve_context_surrender:resolve_context_surrender_then`] to run a callback after the reducer completes.
    fn resolve_context_surrender(
        &self,
        actor_id: u64,
        target_id: u64,
        contact_ref: String,
        action: SurrenderActionKind,
        expected_revision: u32,
        source_id: String,
    ) -> __sdk::Result<()> {
        self.resolve_context_surrender_then(
            actor_id,
            target_id,
            contact_ref,
            action,
            expected_revision,
            source_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `resolve_context_surrender` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn resolve_context_surrender_then(
        &self,
        actor_id: u64,
        target_id: u64,
        contact_ref: String,
        action: SurrenderActionKind,
        expected_revision: u32,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl resolve_context_surrender for super::RemoteReducers {
    fn resolve_context_surrender_then(
        &self,
        actor_id: u64,
        target_id: u64,
        contact_ref: String,
        action: SurrenderActionKind,
        expected_revision: u32,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ResolveContextSurrenderArgs {
                actor_id,
                target_id,
                contact_ref,
                action,
                expected_revision,
                source_id,
            },
            callback,
        )
    }
}
