// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CommitTacticalParticipantSurrenderArgs {
    pub mission_id: String,
    pub character_id: u64,
    pub expected_revision: u32,
    pub source_id: String,
}

impl From<CommitTacticalParticipantSurrenderArgs> for super::Reducer {
    fn from(args: CommitTacticalParticipantSurrenderArgs) -> Self {
        Self::CommitTacticalParticipantSurrender {
            mission_id: args.mission_id,
            character_id: args.character_id,
            expected_revision: args.expected_revision,
            source_id: args.source_id,
        }
    }
}

impl __sdk::InModule for CommitTacticalParticipantSurrenderArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `commit_tactical_participant_surrender`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait commit_tactical_participant_surrender {
    /// Request that the remote module invoke the reducer `commit_tactical_participant_surrender` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`commit_tactical_participant_surrender:commit_tactical_participant_surrender_then`] to run a callback after the reducer completes.
    fn commit_tactical_participant_surrender(
        &self,
        mission_id: String,
        character_id: u64,
        expected_revision: u32,
        source_id: String,
    ) -> __sdk::Result<()> {
        self.commit_tactical_participant_surrender_then(
            mission_id,
            character_id,
            expected_revision,
            source_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `commit_tactical_participant_surrender` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn commit_tactical_participant_surrender_then(
        &self,
        mission_id: String,
        character_id: u64,
        expected_revision: u32,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl commit_tactical_participant_surrender for super::RemoteReducers {
    fn commit_tactical_participant_surrender_then(
        &self,
        mission_id: String,
        character_id: u64,
        expected_revision: u32,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CommitTacticalParticipantSurrenderArgs {
                mission_id,
                character_id,
                expected_revision,
                source_id,
            },
            callback,
        )
    }
}
