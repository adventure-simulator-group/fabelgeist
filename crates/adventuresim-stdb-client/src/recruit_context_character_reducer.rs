// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecruitContextCharacterArgs {
    pub actor_id: u64,
    pub character_id: u64,
    pub destination_party_id: String,
    pub contact_ref: String,
    pub expected_disposition_revision: u32,
    pub source_id: String,
}

impl From<RecruitContextCharacterArgs> for super::Reducer {
    fn from(args: RecruitContextCharacterArgs) -> Self {
        Self::RecruitContextCharacter {
            actor_id: args.actor_id,
            character_id: args.character_id,
            destination_party_id: args.destination_party_id,
            contact_ref: args.contact_ref,
            expected_disposition_revision: args.expected_disposition_revision,
            source_id: args.source_id,
        }
    }
}

impl __sdk::InModule for RecruitContextCharacterArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `recruit_context_character`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait recruit_context_character {
    /// Request that the remote module invoke the reducer `recruit_context_character` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`recruit_context_character:recruit_context_character_then`] to run a callback after the reducer completes.
    fn recruit_context_character(
        &self,
        actor_id: u64,
        character_id: u64,
        destination_party_id: String,
        contact_ref: String,
        expected_disposition_revision: u32,
        source_id: String,
    ) -> __sdk::Result<()> {
        self.recruit_context_character_then(
            actor_id,
            character_id,
            destination_party_id,
            contact_ref,
            expected_disposition_revision,
            source_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `recruit_context_character` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn recruit_context_character_then(
        &self,
        actor_id: u64,
        character_id: u64,
        destination_party_id: String,
        contact_ref: String,
        expected_disposition_revision: u32,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl recruit_context_character for super::RemoteReducers {
    fn recruit_context_character_then(
        &self,
        actor_id: u64,
        character_id: u64,
        destination_party_id: String,
        contact_ref: String,
        expected_disposition_revision: u32,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecruitContextCharacterArgs {
                actor_id,
                character_id,
                destination_party_id,
                contact_ref,
                expected_disposition_revision,
                source_id,
            },
            callback,
        )
    }
}
