// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::legal_owner_kind_type::LegalOwnerKind;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TransferLegalPropertyArgs {
    pub actor_id: u64,
    pub property_id: String,
    pub quantity: u64,
    pub expected_owner_kind: LegalOwnerKind,
    pub expected_owner_id: String,
    pub expected_version: u32,
    pub to_owner_kind: LegalOwnerKind,
    pub to_owner_id: String,
    pub source_id: String,
}

impl From<TransferLegalPropertyArgs> for super::Reducer {
    fn from(args: TransferLegalPropertyArgs) -> Self {
        Self::TransferLegalProperty {
            actor_id: args.actor_id,
            property_id: args.property_id,
            quantity: args.quantity,
            expected_owner_kind: args.expected_owner_kind,
            expected_owner_id: args.expected_owner_id,
            expected_version: args.expected_version,
            to_owner_kind: args.to_owner_kind,
            to_owner_id: args.to_owner_id,
            source_id: args.source_id,
        }
    }
}

impl __sdk::InModule for TransferLegalPropertyArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `transfer_legal_property`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait transfer_legal_property {
    /// Request that the remote module invoke the reducer `transfer_legal_property` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`transfer_legal_property:transfer_legal_property_then`] to run a callback after the reducer completes.
    fn transfer_legal_property(
        &self,
        actor_id: u64,
        property_id: String,
        quantity: u64,
        expected_owner_kind: LegalOwnerKind,
        expected_owner_id: String,
        expected_version: u32,
        to_owner_kind: LegalOwnerKind,
        to_owner_id: String,
        source_id: String,
    ) -> __sdk::Result<()> {
        self.transfer_legal_property_then(
            actor_id,
            property_id,
            quantity,
            expected_owner_kind,
            expected_owner_id,
            expected_version,
            to_owner_kind,
            to_owner_id,
            source_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `transfer_legal_property` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn transfer_legal_property_then(
        &self,
        actor_id: u64,
        property_id: String,
        quantity: u64,
        expected_owner_kind: LegalOwnerKind,
        expected_owner_id: String,
        expected_version: u32,
        to_owner_kind: LegalOwnerKind,
        to_owner_id: String,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl transfer_legal_property for super::RemoteReducers {
    fn transfer_legal_property_then(
        &self,
        actor_id: u64,
        property_id: String,
        quantity: u64,
        expected_owner_kind: LegalOwnerKind,
        expected_owner_id: String,
        expected_version: u32,
        to_owner_kind: LegalOwnerKind,
        to_owner_id: String,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TransferLegalPropertyArgs {
                actor_id,
                property_id,
                quantity,
                expected_owner_kind,
                expected_owner_id,
                expected_version,
                to_owner_kind,
                to_owner_id,
                source_id,
            },
            callback,
        )
    }
}
