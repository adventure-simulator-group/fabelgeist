// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::character_custodian_kind_type::CharacterCustodianKind;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CaptureCharacterArgs {
    pub actor_id: u64,
    pub character_id: u64,
    pub context_id: String,
    pub expected_version: u32,
    pub custodian_kind: CharacterCustodianKind,
    pub custodian_id: String,
    pub ransom_minor: Option<u64>,
    pub source_id: String,
}

impl From<CaptureCharacterArgs> for super::Reducer {
    fn from(args: CaptureCharacterArgs) -> Self {
        Self::CaptureCharacter {
            actor_id: args.actor_id,
            character_id: args.character_id,
            context_id: args.context_id,
            expected_version: args.expected_version,
            custodian_kind: args.custodian_kind,
            custodian_id: args.custodian_id,
            ransom_minor: args.ransom_minor,
            source_id: args.source_id,
        }
    }
}

impl __sdk::InModule for CaptureCharacterArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `capture_character`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait capture_character {
    /// Request that the remote module invoke the reducer `capture_character` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`capture_character:capture_character_then`] to run a callback after the reducer completes.
    fn capture_character(
        &self,
        actor_id: u64,
        character_id: u64,
        context_id: String,
        expected_version: u32,
        custodian_kind: CharacterCustodianKind,
        custodian_id: String,
        ransom_minor: Option<u64>,
        source_id: String,
    ) -> __sdk::Result<()> {
        self.capture_character_then(
            actor_id,
            character_id,
            context_id,
            expected_version,
            custodian_kind,
            custodian_id,
            ransom_minor,
            source_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `capture_character` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn capture_character_then(
        &self,
        actor_id: u64,
        character_id: u64,
        context_id: String,
        expected_version: u32,
        custodian_kind: CharacterCustodianKind,
        custodian_id: String,
        ransom_minor: Option<u64>,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl capture_character for super::RemoteReducers {
    fn capture_character_then(
        &self,
        actor_id: u64,
        character_id: u64,
        context_id: String,
        expected_version: u32,
        custodian_kind: CharacterCustodianKind,
        custodian_id: String,
        ransom_minor: Option<u64>,
        source_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CaptureCharacterArgs {
                actor_id,
                character_id,
                context_id,
                expected_version,
                custodian_kind,
                custodian_id,
                ransom_minor,
                source_id,
            },
            callback,
        )
    }
}
