# Generated by Cargo
# will have compiled files and executables
debug
target
# Pinned Meta MHR authoring assets installed by `just init-mhr-assets` live
# below target/mhr-assets/. Track only deliberately exported runtime artifacts.
/.codex-target-*/
.workbench

# Cascadeur/character-creator GLBs are reproducible authoring exports. Runtime
# animation GLBs remain tracked under assets/animations/.
/assets_src/**/*.glb
!/assets_src/biped/unarmed/base.glb

# Root JavaScript tooling dependencies
/node_modules/

# Local credentials and other developer-only environment settings.
.env
.env.*
!.env.example

# Viabundus source data installed by `just init-world-data`.
/viabundus/

# Downloaded LUH1 source archives. Extract the required NetCDF files into the
# configured local world-data source directory instead of committing the archive.
/Land_Use_Harmonization_*.zip

# These are backup files generated by rustfmt
**/*.rs.bk

# Character creator, MHR, and NumPy storage have independently built dependency
# graphs. Keep their lockfiles committed so CI uses the same resolved graph as
# developers.

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Dev tools - ignore everything except helper scripts
utils/*
!utils/*.py

# Obsidian
wiki/.obsidian/
.obsidian

# Direnv
.direnv

# Mdbook
book
/index.html

# Generated browser build output
crates/adventuresim-stdb-module/static/wasm/
crates/adventuresim-stdb-module/static/assets/

# Local browser inspection and development output
.playwright-cli/
output/
**/__pycache__/
*.stdout.log
*.stderr.log
crates/strategic-web/node_modules/
